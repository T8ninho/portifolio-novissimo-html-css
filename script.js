

// const myObserver = new IntersectionObserver((entidades) => {
// 	entidades.forEach((entrada) => {
// 		if(entrada.isIntersecting) {
// 			entrada.target.classList.add('exibir')
// 		}
// 	})
// })

// const elementos = document.querySelectorAll('.oculto')

// elementos.forEach( (elemento) => myObserver.observe(elemento))